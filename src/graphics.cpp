//#include "graphics.hh"

//void Graphics::createLine(GPU_Target* target, float x1, float y1, float x2, float y2, SDL_Color color) {
//    GPU_Line(target, x1, y1, x2, y2, color);
//}

//void Graphics::createCirculo(GPU_Target* target, float x, float y, float radius, SDL_Color color, bool fill) {
//    if (fill) GPU_Circle(target, x, y, radius, color);
//    else GPU_CircleFilled(target, x, y, radius, color);
//}

//void Graphics::createTriangulo(GPU_Target* target, float x1, float y1, float x2, float y2, float x3, float y3, SDL_Color color, bool fill) {
//    if (fill) GPU_Tri(target, x1, y1, x2, y2, x3, y3, color);
//    else GPU_TriFilled(target, x1, y1, x2, y2, x3, y3, color);
//}

//void Graphics::createRetangulo(GPU_Target* target, float x1, float y1, float x2, float y2, SDL_Color color, bool fill) {
//    if (fill) GPU_Rectangle(target, x1, y1, x2, y2, color);
//    else GPU_RectangleFilled(target, x1, y1, x2, y2, color);
//}

//void Graphics::createRetangulo(GPU_Target* target, GPU_Rect rect, SDL_Color color, bool fill) {
//    if (fill) GPU_Rectangle2(target, rect, color);
//    else GPU_RectangleFilled2(target, rect, color);
//}



